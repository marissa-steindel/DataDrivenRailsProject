<div class="container" >
  <h1>All Books - Collection</h1>

  <% if @books.empty? %>
    <p>There are no books.</p>
  <% else %>
    <ul>
      <% @books.each do |book| %>
        <li> <%= link_to book.title, book_path(book) %> by <%= link_to book.author.name, book.author %></li>
      <% end %>
    </ul>
  <% end %>
</div>