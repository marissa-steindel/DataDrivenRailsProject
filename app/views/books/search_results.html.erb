<h1>Search Results</h1>

<p>You searched for '<%= params[:query] %>'.</p>
<p><%= pluralize(@books.size, "result")%> found.</p>

<% if @books.empty? %>
  <p>No books found.</p>
<% else %>
  <ul>
    <% @books.each do |book| %>
      <li> <%= link_to book.title, book %> </li>
    <% end %>
  </ul>
<% end %>